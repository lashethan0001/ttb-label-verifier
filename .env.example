 # Server-side only — never exposed to the browser.
   ANTHROPIC_API_KEY=sk-ant-your-key-here

   # Optional: if running the frontend without `vercel dev`, point it at a
   # deployed API origin, e.g. https://your-app.vercel.app
   # VITE_API_BASE=
